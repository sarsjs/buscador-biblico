// Este es un service worker básico.
// Por ahora, solo es necesario para que la PWA sea instalable
// y para que Bubblewrap funcione.
self.addEventListener('fetch', event => {
  // No hace nada por ahora, pero su existencia es clave.
});

